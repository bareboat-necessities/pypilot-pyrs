use std::*;
use std::collections::HashMap;

use config_esp32::{config};
let try_dummy = { //unsupported
};
let except!(Exception) = { //unsupported
println ! ("{:?} {:?} ", "exception", e);
println ! ("{:?} ", "reboot");
machine.reset();
};