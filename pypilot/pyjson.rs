use std::*;
use std::collections::HashMap;

let try_dummy = { //unsupported
let (loads, dumps) = (ujson.loads, ujson.dumps);
};
let except!(Exception) = { //unsupported
println ! ("{:?} {:?} ", _("WARNING: python ujson library failed, parsing will consume more cpu"), e);
let (loads, dumps) = (json.loads, json.dumps);
};